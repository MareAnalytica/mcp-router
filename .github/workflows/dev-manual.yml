# .github/workflows/dev-manual.yml
# ─────────────────────────────────────────────────────────────
# Manually build + deploy any branch to dev.
# Trigger from: Actions tab → "Deploy Branch → Dev" → Run workflow → pick branch
# ─────────────────────────────────────────────────────────────
name: Deploy Branch → Dev

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      images: >-
        [
          { "name": "mcp-router-backend",  "context": "./backend",  "dockerfile": "./backend/Dockerfile" },
          { "name": "mcp-router-frontend", "context": "./frontend", "dockerfile": "./frontend/Dockerfile" }
        ]

  deploy:
    needs: build
    uses: ./.github/workflows/_deploy.yml
    with:
      environment: dev
      namespace: mcp-router-dev
      deployments: "backend,frontend"
